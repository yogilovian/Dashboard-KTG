rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all
    match /{document=**} {
      allow read, write: if false;
    }

    // Dashboard operasional stasiun
    match /stations/{stationId} {
      allow read, write: if true;
    }
  }
}
